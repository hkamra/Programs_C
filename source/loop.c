#include <string.h>
#include <stdio.h>

/*void load_buffer(char *buffer);

int main()
{   char val;
    char buffer[100];
    printf("buffer size: %d\n", sizeof(buffer));
    val = load_buffer(buffer);
    printf("buffer size: %d\n", val);
    return 0;
}

int load_buffer(char *buffer)
{   char val;
    printf("sizeof(buffer): %d\n", sizeof(buffer));
    val = sizeof(buffer);
    return val;
} */

int main(){
int m;
 for (m = 0; m < 2 ; m++){

                m++;
            }
            printf("%d",m);

            return 0;
            getch();
}
